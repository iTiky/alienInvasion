package logging

const (
	// ServiceKey defines the logging key for tracking the service log originated from.
	ServiceKey = "service"

	// CityNameKey defines the logging key for tracking the City ID.
	CityNameKey = "city"

	// AlienNameKey defines the logging key for tracking the Alien ID.
	AlienNameKey = "alien"
)
